apiVersion: v1
kind: ConfigMap
metadata:
  namespace: goproject
  name: goproject-usercenter
data:
  usercenter.yaml: |
    environment:
    debug: false
    rpcServerAddr:
    db:
      addr:
      user:
      password:
      database:
    redis:
      addr:
      password:
